import React from 'react';

// This defines our Dashboard component.
// It's a simple function that returns what the page should look like.
function Dashboard() {
  return (
    // JSX code representing the page's structure
    <div>
      <h1>Dashboard</h1>
      <p>Welcome to the dashboard! This is a placeholder for now.</p>
    </div>
  );
}

// We export the component so we can use it in other files (like App.jsx)
export default Dashboard;